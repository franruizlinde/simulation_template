
// end of main()
export function launchCustomFeatures()
{
    // insert here starting functions for all features that needs to be launched in main()
    
    setupInitialStateOfShowRoom();
}

// inside DEVICE_onInputChange()
export function DEVICE_onInputChange(idDevice, inPin, newState, lastStateDuration)
{
    // insert here code to be executed on a Input change of a device
}