export function customEventsList = events_populate(eventList)
{
    customEventsList = {};
    /*
        // Here are some example of how adding events to your project
        
        // Launched on the RM for all robots
        customEventsList.{end+1} = varspc('category','comm','name','example1','broadcast',1,'linkOnBcst',{strn('onExample1')}), 
        
        // Launched either from the RM to a specific robot or the opposit  
        customEventsList.{end+1} = varspc('category','comm','name','example2','linkTo',{strn('onExample2')}),
    */
}